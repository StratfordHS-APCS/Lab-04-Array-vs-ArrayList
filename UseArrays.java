/**
 * Write a description of class UseArrays here.
 *
 * @author (name)
 * @version (date)
 */
public class UseArrays
{  
    public static int[] allPrimesUpTo( int max )
    {
        // Using a loop, make an array of all the prime numbers from 2 - max
        // must grow the array one element at a time, don't precalculate the size you need.
        // Return the array of primes.
    }
    
    public static int[] squareEachElement( int[] list )
    {
        // square each element in list. Return the new list.
    }
    
    public static int[] removeElement( int[] list, int element )
    {
        // remove element number "element" from list.  Return the new list.
    }
    
    public static void printArray( int[] array )
    {
        // using an enhanced for loop to nicely print the array
    }
    
    public static void main( String[] args )
    {
        // you may modify anything in this method without failing the tests.
        // Remember to send clones of the arrays to the methods so the original array
        // is not destroyed (changed).
        System.out.println( "Getting all prime numbers through 100..." );
        // call allPrimesUpTo() and store the result as a variable
        // print the result using printArray()
        System.out.println( "Squaring all of the primes..." );
        // call squareEachElement()
        // print the list of squared elements using printArray()
        System.out.println( "Removing the 4th prime number..." );
        // call removeElement() to remove an element from an array.
        // print the result using printArray()
    }
}
